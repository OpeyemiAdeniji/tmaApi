import fs from 'fs'
import colors from 'colors'

// import Food from '../../models/Food.model'

// read in the JSON file
// const foods = JSON.parse(
//     fs.readFileSync(`${__dirname.split('config')[0]}_data/example.json`, 'utf-8')
// )

export const seedCategories = async (): Promise<void> => {

    // try {

    //     const u = await Food.find({}); 
    //     if(u && u.length > 0) return;

    //     const seed = await Food.create(foods);

    //     if(seed){
    //         console.log(colors.green.inverse('Food items seeded successfully'))
    //     }
        
    // } catch (err) {

    //     console.log(colors.red.inverse(`${err}`))
        
    // }

}